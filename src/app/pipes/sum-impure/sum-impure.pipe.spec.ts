import { SumImpurePipe } from './sum-impure.pipe';

describe('SumImpurePipe', () => {
  it('create an instance', () => {
    const pipe = new SumImpurePipe();
    expect(pipe).toBeTruthy();
  });
});
